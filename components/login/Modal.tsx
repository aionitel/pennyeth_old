import ReactModal from 'react-modal'
import React from 'react'

const Modal: React.FC = () => {
  return (
    <div>
      <ReactModal 
        isOpen={true}
      />
    </div>
  )
}

export default Modal
