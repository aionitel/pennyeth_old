import React from 'react'
import Modal from '../login/Modal'

const LoggedOutHeader = () => { // Connect button that allows for user to connect eth or sol wallet with modal
  return (
    <div>Logged Out</div>
  )
}

export default LoggedOutHeader